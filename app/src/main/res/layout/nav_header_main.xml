<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_header"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="@color/cardview_shadow_start_color"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackgroundBorderless"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="@dimen/commo_paddi_default"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    tools:context=".home.HomeActivity">

    <ImageView
        android:id="@+id/nav_header_imView"
        android:layout_width="@dimen/commo_paddi_max"
        android:layout_height="@dimen/commo_paddi_max"
        android:src="@mipmap/ic_launcher_foreground"/>

    <TextView
        android:id="@+id/nav_header_tView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/commo_paddi_default"
        android:text="@string/app_name"
        android:textAppearance="@style/TextAppearance.AppCompat.Title" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/commo_paddi_default"
        android:text="@string/email"/>

</LinearLayout>